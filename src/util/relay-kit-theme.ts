/**
 * Compatibility shim for the old RelayProvider import.
 * The board restyle lives in HomeBoard/relay-board.
 */
export { relayBoardTheme as theme } from "@/app/[locale]/_components/HomeBoard/relay-board";
